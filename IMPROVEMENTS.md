# FAT 앱 개선 항목

## 버그

### [x] 1. 정책 변경이력 API data_type 불일치 (긴급)
- **원인**: 싱크 태스크가 `data_type="policies"`로 저장 → API가 `"Policy"`로 조회 → 항상 빈 결과
- **수정**: `firewall_query.py` 3개 엔드포인트 필터 값 `"policies"`로 통일

---

## UX 개선

### [x] 2. 분석 실패 에러 메시지 표시
- **수정**: `AnalysisTask` 모델에 `error_message` 컬럼 추가 (Alembic 마이그레이션)
- 분석 실패 시 에러 메시지 DB 저장, UI에서 실패 상태 아래 에러 텍스트 표시

### [ ] 3. 오브젝트 참조 정책 수 컬럼
- **현황**: Objects 페이지에서 각 오브젝트가 몇 개 정책에서 쓰이는지 알 수 없음
- **수정**: 테이블에 "사용 정책 수" 컬럼 추가, 0이면 빨간 배지

### [x] 4. 분석 결과 → 정책 페이지 딥링크
- **수정**: 분석 결과 정책명 클릭 → `/policies?rule_name=<name>` 이동
- PoliciesPage에 `rule_name` URL 파라미터 지원 추가

### [x] 5. 설정 페이지 삭제 확인 다이얼로그
- **확인 결과**: 사용자 삭제/로그 삭제 모두 이미 confirm 다이얼로그 구현됨. 리스크 포트 삭제는 로컬 상태 변경 → 저장 버튼으로 확정 (확인 불필요)

### [x] 6. 정책 검색 조건 프리셋 저장
- **수정**: localStorage 기반 프리셋 저장/불러오기/삭제 UI 추가
- "저장" 버튼(조건 있을 때), "프리셋" 드롭다운(저장된 프리셋 있을 때)

### [x] 7. 대시보드 주간 변경 통계 차트
- **수정**: `/change-stats` API 연결, ApexCharts 스택 바 차트 추가 (신규/변경/삭제 색상 구분)
